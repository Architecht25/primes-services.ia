<<div class="max-w-6xl mx-auto px-4 py-8">
  <div class="text-center mb-8">
    <div class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4">
      <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>
    <h1 class="text-3xl font-bold text-gray-900 mb-4">
      Demande envoyée avec succès !
    </h1>
    <p class="text-lg text-gray-600 mb-6">
      Voici vos suggestions personnalisées de primes et subsides
    </p>
    <div class="inline-flex items-center bg-blue-50 rounded-full px-4 py-2">
      <span class="text-sm font-medium text-blue-800">
        Référence : #<%= @contact.id %>
      </span>
    </div>
  </div>

  <div class="grid lg:grid-cols-3 gap-8">
    <!-- Colonne principale : Primes suggérées -->
    <div class="lg:col-span-2">
      <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
        <h2 class="text-2xl font-bold text-gray-900 mb-4 flex items-center">
          <svg class="w-6 h-6 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
          </svg>
          Primes et Aides Disponibles
        </h2>

        <% if @suggested_subsidies && @suggested_subsidies.any? %>
          <div class="space-y-4">
            <% @suggested_subsidies.each_with_index do |subsidy, index| %>
              <div class="border border-gray-200 rounded-lg p-4 hover:border-blue-300 transition-colors">
                <div class="flex items-start justify-between">
                  <div class="flex-1">
                    <h3 class="text-lg font-semibold text-gray-900 mb-2">
                      <%= subsidy %>
                    </h3>
                    <div class="flex items-center text-sm text-gray-500 space-x-4">
                      <span class="flex items-center">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                        </svg>
                        <%= @contact.region.humanize %>
                      </span>
                      <span class="flex items-center">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        </svg>
                        Priorité <%= index < 3 ? 'Élevée' : 'Moyenne' %>
                      </span>
                    </div>
                  </div>
                  <div class="ml-4 flex-shrink-0">
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                      Éligible
                    </span>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-8">
            <svg class="w-12 h-12 text-gray-400 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
            </svg>
            <p class="text-gray-500">Analyse en cours... Vous recevrez les suggestions par email.</p>
          </div>
        <% end %>
      </div>

      <!-- Message personnalisé IA -->
      <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg p-6">
        <h3 class="text-xl font-bold text-gray-900 mb-4 flex items-center">
          <svg class="w-6 h-6 text-indigo-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
          </svg>
          Analyse Personnalisée IA
        </h3>
        <div class="prose prose-blue max-w-none">
          <% if @personalized_message %>
            <%= simple_format(@personalized_message, class: "text-gray-700 leading-relaxed") %>
          <% else %>
            <p class="text-gray-700">Notre intelligence artificielle analyse votre profil pour générer des recommandations personnalisées. Vous recevrez une analyse détaillée par email sous 24h.</p>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Colonne latérale : Informations et actions -->
    <div class="lg:col-span-1">
      <!-- Prochaines étapes -->
      <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">Prochaines étapes</h3>
        <div class="space-y-3">
          <div class="flex items-start">
            <div class="flex-shrink-0 w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center mt-0.5">
              <span class="text-xs font-bold text-blue-600">1</span>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-gray-900">Email de confirmation</p>
              <p class="text-sm text-gray-500">Envoyé à <%= @contact.email %></p>
            </div>
          </div>
          <div class="flex items-start">
            <div class="flex-shrink-0 w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center mt-0.5">
              <span class="text-xs font-bold text-blue-600">2</span>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-gray-900">Analyse IA approfondie</p>
              <p class="text-sm text-gray-500">Sous 24h</p>
            </div>
          </div>
          <div class="flex items-start">
            <div class="flex-shrink-0 w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center mt-0.5">
              <span class="text-xs font-bold text-blue-600">3</span>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-gray-900">Contact expert</p>
              <p class="text-sm text-gray-500">Si projet complexe</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Résumé du contact -->
      <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">Votre profil</h3>
        <dl class="space-y-2">
          <div>
            <dt class="text-sm font-medium text-gray-500">Type</dt>
            <dd class="text-sm text-gray-900">
              <%= case @contact.type
                  when 'ParticulierContact'
                    'Particulier'
                  when 'AcpContact'
                    'Copropriété (ACP)'
                  when 'EntrepriseImmoContact'
                    'Entreprise Immobilière'
                  when 'EntrepriseCommContact'
                    'Entreprise Commerciale'
                  end %>
            </dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">Région</dt>
            <dd class="text-sm text-gray-900"><%= @contact.region.humanize %></dd>
          </div>
          <% if @contact.estimated_budget %>
            <div>
              <dt class="text-sm font-medium text-gray-500">Budget estimé</dt>
              <dd class="text-sm text-gray-900"><%= number_to_currency(@contact.estimated_budget, unit: '€', precision: 0) %></dd>
            </div>
          <% end %>
          <div>
            <dt class="text-sm font-medium text-gray-500">Date de demande</dt>
            <dd class="text-sm text-gray-900"><%= @contact.created_at.strftime('%d/%m/%Y à %H:%M') %></dd>
          </div>
        </dl>
      </div>

      <!-- Actions -->
      <div class="bg-white rounded-lg shadow-lg p-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">Actions</h3>
        <div class="space-y-3">
          <button onclick="toggleChatbot()" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg transition-colors">
            💬 Poser une question
          </button>
          <%= link_to new_contact_path, class: "w-full bg-gray-100 hover:bg-gray-200 text-gray-900 font-semibold py-2 px-4 rounded-lg transition-colors text-center block" do %>
            📝 Nouvelle demande
          <% end %>
          <a href="mailto:<%= @contact.email %>?subject=Suivi demande primes #<%= @contact.id %>" class="w-full bg-gray-100 hover:bg-gray-200 text-gray-900 font-semibold py-2 px-4 rounded-lg transition-colors text-center block">
            📧 Modifier email
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Redirection Ren0vate si applicable -->
  <% if @contact.respond_to?(:requires_expert_assistance?) && @contact.requires_expert_assistance? %>
    <div class="mt-8 bg-gradient-to-r from-purple-50 to-pink-50 rounded-lg p-6">
      <div class="text-center">
        <h3 class="text-xl font-bold text-gray-900 mb-4">
          🚀 Projet Complexe Détecté
        </h3>
        <p class="text-gray-700 mb-6">
          Notre IA a identifié que votre projet pourrait bénéficier d'un accompagnement expert personnalisé.
          Ren0vate, notre partenaire spécialisé, peut vous accompagner dans l'optimisation de vos primes.
        </p>
        <div class="flex justify-center space-x-4">
          <a href="#" class="bg-purple-600 hover:bg-purple-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors inline-flex items-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
            </svg>
            Être accompagné par un expert
          </a>
          <button class="bg-gray-200 hover:bg-gray-300 text-gray-900 font-semibold py-3 px-6 rounded-lg transition-colors">
            Plus tard
          </button>
        </div>
      </div>
    </div>
  <% end %>
</div>

<script>
// Intégration avec le chatbot existant pour support post-soumission
function toggleChatbot() {
  const chatWidget = document.querySelector('[data-ai-chatbot-target="widget"]');
  if (chatWidget) {
    chatWidget.classList.remove('hidden');
    // Contexte post-soumission
    const contextMessage = `J'ai soumis une demande (référence #<%= @contact.id %>) et j'aimerais des clarifications sur les primes suggérées.`;

    window.dispatchEvent(new CustomEvent('ai-chat-context', {
      detail: {
        message: contextMessage,
        contactId: '<%= @contact.id %>',
        contactType: '<%= @contact.type %>'
      }
    }));
  }
}
</script>v>
  <h1 class="font-bold text-4xl">Contacts#show</h1>
  <p>Find me in app/views/contacts/show.html.erb</p>
</div>
